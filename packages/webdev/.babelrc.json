{
  "presets": [
    [
      "@babel/preset-env",
      {
        "bugfixes": true,
        "targets": {
          "browsers": [
            "last 1 year",
            "not dead",
            "not opera < 1000",
            "not op_mob < 1000",
            "not Samsung < 1000"
          ]
        }
      }
    ],
    "@babel/preset-typescript",
    [
      "@babel/preset-react",
      {
        "runtime": "automatic",
        "allowDeclareFields": true
      }
    ]
  ]
}